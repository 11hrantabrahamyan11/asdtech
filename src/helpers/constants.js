import localStorage from './localStorage';

// App
export const APP_BASE_NAME = 'ASDTEST__HRANT';

// API
export const API_BASE_URL = 'https://work.vint-x.net/api';

// LocalStorage keys
export const LOCAL_STORAGE_KEYS = {
  token: localStorage.createKey('token'),
};
